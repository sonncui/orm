The project is for Fluent Nhibernate study.

The master branch is the base code include the config of Nhibernate.

The example branch has done the base operations and the nested operation using Nhibernate.

CreateExampleDataBase.sql  is the script using to create the database our project rely on.

The environment to execute the project :


*Visual Studio 
*Sql Server 